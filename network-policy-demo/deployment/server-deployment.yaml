apiVersion: apps/v1
kind: Deployment

metadata:
  name: server
  namespace: demo

spec:
  replicas: 1

  selector:
    matchLabels:
      app: server

  template:
    metadata:
      labels:
        app: server

    spec:
      containers:
      - name: nginx
        image: nginx
        ports:
        - containerPort: 80